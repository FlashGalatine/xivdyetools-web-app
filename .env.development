# Local development environment variables
# These override defaults during `npm run dev`

# Use local OAuth worker for testing
VITE_OAUTH_WORKER_URL=http://localhost:8788

# Use local Universalis proxy for testing (run: cd xivdyetools-universalis-proxy && npm run dev)
# Uncomment to use local proxy, or leave commented to use production proxy
# VITE_UNIVERSALIS_PROXY_URL=http://localhost:8787/api/v2
